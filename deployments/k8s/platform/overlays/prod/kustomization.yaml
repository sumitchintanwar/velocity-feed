apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - hpa.yaml
  - pdb.yaml

# Production-specific overlays (e.g. patching replica counts if HPA is disabled, or changing image tags)
