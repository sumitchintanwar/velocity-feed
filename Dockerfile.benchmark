FROM golang:1.23-alpine AS builder
ENV GOTOOLCHAIN=auto
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /bench ./cmd/benchmark

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /bench /bench
ENTRYPOINT ["/bench"]
